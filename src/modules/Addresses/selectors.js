export const getErrorText = state => state.addresses.errorText;
export const getAddresses = state => state.addresses.addresses;
export const getIsLoadingAddresses = state => state.addresses.isLoadingAddresses;