import { createAction } from 'redux-actions';

export const handleProfileSubmit = createAction('PROFILE/HANDLE_PROFILE_SUBMIT');
export const handleProfileClear = createAction('PROFILE/HANDLE_PROFILE_CLEAR');